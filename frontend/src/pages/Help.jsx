// Простая инструкция по панели для не-технических пользователей (RU).
export default function Help() {
  return (
    <div className="guide">
      <div className="page-head">
        <div>
          <h1>Инструкция</h1>
          <p>Как пользоваться панелью. Коротко и по шагам.</p>
        </div>
      </div>

      <fieldset className="settings-section" style={{ marginBottom: 16 }}>
        <legend>Что это такое</legend>
        <p>
          Панель — это <b>RADIUS-прокси</b>. Устройства (VPN, UAG, WiFi) шлют сюда
          запрос «пустить ли пользователя». Панель по правилам решает, <b>куда</b>{" "}
          переслать запрос (например на сервер 2FA), при желании проверяет, что
          пользователь <b>состоит в нужной группе AD</b>, и пересылает. Всё, что
          происходит, видно в разделе <b>Logs</b>.
        </p>
        <p className="guide-flow">
          Клиент (устройство) → Правило → Пул → Целевой сервер (2FA)
        </p>
      </fieldset>

      <fieldset className="settings-section" style={{ marginBottom: 16 }}>
        <legend>Быстрый старт — по порядку</legend>
        <ol className="guide-steps">
          <li>
            <b>Target servers</b> (куда пересылаем). Добавь сервер 2FA: IP/хост,
            порт (обычно 1812) и <b>shared secret</b>. Секрет должен совпадать с тем,
            что настроен на самом сервере 2FA.
          </li>
          <li>
            <b>Pools</b> (группа серверов). Создай пул и положи в него нужный
            target-сервер. Правила пересылают в пул, а не в сервер напрямую (так можно
            резервировать несколько).
          </li>
          <li>
            <b>Clients</b> (от кого принимаем). Добавь устройство: его <b>IP</b> и{" "}
            <b>shared secret</b>. Важно: IP — тот, <b>каким его видит наш сервер</b>{" "}
            (если между вами NAT — это адрес после NAT). Секрет — точно как на
            устройстве. Запрос с неизвестного IP панель молча отбрасывает.
          </li>
          <li>
            <b>Rules</b> (правила маршрутизации). Правило: «от клиента X (можно ещё по
            имени пользователя) → в пул Y». Проверяются сверху вниз, срабатывает первое
            подходящее. Хочешь проверять группу AD — включи галку «Require AD group
            membership» и выбери группу.
          </li>
          <li>
            <b>Config &amp; apply</b> — нажми, чтобы записать настройки в FreeRADIUS.
            Панель проверит конфиг и перезагрузит сервер. Без этого шага изменения не
            вступают в силу.
          </li>
        </ol>
      </fieldset>

      <fieldset className="settings-section" style={{ marginBottom: 16 }}>
        <legend>Проверка группы AD</legend>
        <p>
          Если нужно пускать только членов определённой группы Active Directory:
        </p>
        <ol className="guide-steps">
          <li>
            <b>Settings → AD / LDAP</b> — укажи подключение к контроллеру домена
            (сервер, учётка для чтения, Base DN) и включи. Кнопкой можно проверить связь.
            Панель периодически подтягивает состав групп к себе.
          </li>
          <li>
            В нужном <b>правиле</b> (Rules → Edit) включи «Require AD group membership»
            и начни вводить имя группы — панель подскажет из синхронизированных.
          </li>
        </ol>
      </fieldset>

      <fieldset className="settings-section" style={{ marginBottom: 16 }}>
        <legend>Где смотреть, что происходит — Logs</legend>
        <ul className="guide-list">
          <li>
            <b>Decisions</b> — то, что панель <b>обработала и переслала</b>: кто, куда
            (realm/пул), результат проверки группы, ответ (Accept/Reject).
          </li>
          <li>
            <b>Server log</b> — «сырой» лог FreeRADIUS: то, что отбросили <b>до</b>{" "}
            правил. Тут видно <b>«unknown client»</b> с реальным IP (если устройство
            шлёт с неожиданного адреса) и <b>неверный секрет</b>. Есть фильтр по тексту —
            вбей IP устройства и увидишь, дошёл ли запрос и почему отклонён.
          </li>
        </ul>
      </fieldset>

      <fieldset className="settings-section" style={{ marginBottom: 16 }}>
        <legend>Настройки (Settings)</legend>
        <ul className="guide-list">
          <li><b>RADIUS</b> — «Max request time». Для медленного 2FA (подтверждение в приложении дольше 30 сек) подними это значение, иначе запрос отвалится по таймауту.</li>
          <li><b>TLS</b> — сертификат самой панели (HTTPS). По умолчанию самоподписанный; можно загрузить свой файлами.</li>
          <li><b>Access</b> — включить вход по паролю в панель и ограничить доступ по IP.</li>
          <li><b>Host</b> — имя и адреса сервера (только просмотр).</li>
        </ul>
        <p className="field-hint">
          Пароль администратора меняется в меню с именем пользователя (правый верхний угол).
        </p>
      </fieldset>

      <fieldset className="settings-section" style={{ marginBottom: 16 }}>
        <legend>Перенос настроек и бэкап</legend>
        <ul className="guide-list">
          <li><b>Import / Export</b> — выгрузить/загрузить всю конфигурацию (клиенты, серверы, пулы, правила) одним JSON-файлом. При загрузке сначала показывается план, потом «Применить». Удобно для переноса на другой сервер.</li>
          <li>На самом сервере есть команда <span className="mono">sudo rpp</span> — меню: обновление, бэкап/восстановление БД, статус, логи, смена пароля.</li>
        </ul>
      </fieldset>

      <fieldset className="settings-section">
        <legend>Если «логин проходит/не проходит, а в логах пусто»</legend>
        <ol className="guide-steps">
          <li>Открой <b>Logs → Server log</b>, вбей в фильтр IP устройства.</li>
          <li>Видишь <b>«unknown client»</b> — значит запрос пришёл с адреса, которого нет в <b>Clients</b>. Добавь клиента с этим IP (учитывай NAT).</li>
          <li>Видишь про <b>Message-Authenticator / invalid</b> — не совпадает <b>секрет</b> между устройством и клиентом в панели.</li>
          <li>Пусто совсем — запрос не доходит: проверь на устройстве адрес и порт нашего сервера (1812/udp) и сеть/файрвол.</li>
        </ol>
      </fieldset>
    </div>
  );
}
